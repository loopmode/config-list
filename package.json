{
  "private": true,
  "name": "editable-list-workspace",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/example && yarn start",
    "build": "lerna run build --ignore example --stream",
    "watch": "lerna run watch --ignore example --stream",
    "lint": "lerna run lint --ignore example --stream",
    "docs": "lerna run docs --ignore example"
  },
  "devDependencies": {
    "@loopmode/babel7-base": "^1.0.1"
  }
}
